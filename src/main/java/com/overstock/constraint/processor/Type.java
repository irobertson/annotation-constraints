package com.overstock.constraint.processor;

/**
 * Marker interface for types, which can either by classes (represented by {@link ClassElement}),
 * primitives (represented by {@link Primitive}), or arrays (represented by {@link Array}).
 */
public interface Type {}
